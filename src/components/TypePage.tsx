import React, { useEffect, useRef, useState } from 'react';
import styled from 'styled-components'

const Container = styled.div`
    width: 100%;
    top: 0px;
    background: blue;
`

const Heading = styled.div`
    width: 100%;
    top: 0px;
    background: blue;
`


interface propTypes {
 
}
function TypePage(props: propTypes) {

    return (
        <Container
            style={{
    
            }}
        >
           

        </Container>
    );
}

export default TypePage;